<section class="lg:px-16 px-4 text-gray-600 dark:text-gray-400 bg-gray-100 dark:bg-dark body-font">
  <div class="container mx-auto flex px-5 py-24 md:flex-row flex-col items-center">
    <div class="lg:flex-grow md:w-1/2 lg:pr-24 md:pr-16 flex flex-col md:items-start md:text-left mb-16 md:mb-0 items-center text-center">
      <h1 class="title-font text-5xl mb-4 font-medium text-gray-800 dark:text-gray-100">{{@site.title}}</h1>
      <p class="mb-8 text-xl">{{@site.description}}</p>
    </div>
    <div class="md:w-1/2 w-full">
      {{#if @site.cover_image}}
      <img class="object-cover rounded-lg" alt="hero" src="{{@site.cover_image}}">
      {{/if}}
    </div>
  </div>
</section>